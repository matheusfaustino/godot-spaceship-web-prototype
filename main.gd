extends Node2D

func _ready() -> void:
	# fixed screen issue with fractional scale instead of this hack
	
	#var real_size = ScreenScale.calculate_real_size()
	#ScreenScale.apply_scale_to_window(real_size)
	
	#if real_size:
		#$Control.size = real_size
	pass
